class Member {
  String username;
  int age;
  String gender;
  DateTime startDate;
  DateTime endDate;
  String avatar;
  String premiumId;

  Member(this.username, this.age, this.gender,  this.startDate, this.endDate, this.avatar, this.premiumId);
}