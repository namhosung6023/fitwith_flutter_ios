class Beta {
  String id;
  String title;
  String contents;
  String type;
  String userId;
  String username;
  DateTime createdAt;

  Beta(this.id, this.title, this.contents, this.type, this.userId,
      this.username, this.createdAt);
}
