class Beta {
  String id;
  String title;
  String contents;
  String type;
  String username;
  DateTime createdAt;

  Beta(this.id, this.title, this.contents, this.type, this.username,
      this.createdAt);
}
